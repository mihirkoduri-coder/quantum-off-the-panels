---
// site copy is edited in place now (see public/admin-edit.js) — this just
// catches anyone with the old form URL bookmarked.
export const prerender = false;
return Astro.redirect("/admin");
---
