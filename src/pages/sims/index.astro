---
import Base from "../../layouts/Base.astro";
import Gallery from "../../components/Gallery";
---
<Base title="Simulations — Quantum off the Panels">
  <div class="wrap">
    <h1>Simulations</h1>
    <p class="dim" style="max-width: 34rem">
      Each one unlocks when you meet it inside its post. That's not a gate for
      its own sake — most of them only make sense after the argument that sets
      them up.
    </p>
    <Gallery client:load />
  </div>
</Base>
